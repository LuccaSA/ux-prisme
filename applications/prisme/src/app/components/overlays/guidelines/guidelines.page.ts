import { FeaturePage } from '@prisme/common';
import { GuidelinesFeature } from './guidelines.feature';

export const overlayGuidelinesPage = new FeaturePage(
	'guidelines',
	'Guidelines',
	GuidelinesFeature,
);
